# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node / TS
node_modules/
dist/
build/
.next/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
*.tsbuildinfo

# Env / secrets — must never be committed
.env
.env.*
!.env.example
.secrets/
.secret/
*.key
*.pem
*.p12
*.pfx

# bee / wasp local state
.bee/
.wasp/

# Test output
test-results/
playwright-report/

# Source/working art (large iteration PNGs); the optimized web asset lives in
# apps/pythia/public/ and IS tracked.
.media/

# Generated runtime stats
pythia-stats.json

# generated runtime stores (volume-backed in prod)
pythia-connectors.json
pythia-settings.json
pythia-txsenders.json

# local dev OIDC env — holds a localhost-only OIDC client; NEVER commit (repo is
# public). Loaded by scripts/localhost-dev.mjs (the `npm run dev` start path).
*.local.env
